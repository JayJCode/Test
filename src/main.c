#include <stdio.h>
int main() {
   // printf() displays the string inside quotation
   int numer_one, numer_two;
   scanf(%d %d, &number_one, &number_two);
   printf("Suma liczb wynosi: %d\n", number_one + number_two);
   return 0;
}
